"""
Wrappers for :mod:`tensorflow`.


This module loads corresponding modules in ``tensorflow`` on demand, just
as ``tensorflow`` does. Its contents might depend on those of the ``tensorflow``
in your system when it is loaded.

"""
