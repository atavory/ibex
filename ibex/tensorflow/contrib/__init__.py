"""
Wrappers for :mod:`tensorflow.contrib`.


This module loads corresponding modules in ``tensorflow.contrib`` on demand, just
as ``tensorflow`` does. Its contents might depend on those of the ``tensorflow.contrib``
in your system when it is loaded.

"""
